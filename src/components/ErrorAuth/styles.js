import { makeStyles } from "@material-ui/core";

export default makeStyles( () => ({
    container: {
        display: 'flex',
        justifyContent: 'space-between',
        minheight:  '50px',
        marginTop: '30px',
        marginBottom: '20px',
        },
    alert: {
        width: '100%',
    }
}));